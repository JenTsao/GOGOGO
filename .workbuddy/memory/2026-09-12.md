# 2026-09-12

- 完成高考副驾驶项目全貌分析：读取 README/AGENTS/项目书/schema.sql/bug_review_report/vercel.json，产出架构总览图与分析报告（结论：Phase 1-5 完成，唯一阻塞项为 pnpm-lock.yaml 失同步待重建；次要风险 storage.objects 桶策略未验证）。

- 修复 CI/Vercel 部署阻塞：根 package.json 补 pnpm.overrides（react/react-dom 18.2.0、react-native-screens ~3.31.1、safe-area-context 4.10.5）与 pnpm-lock.yaml 对齐；ci.yml 新增 lockfile-check job（frozen+lockfile-only 守门）。根因：manifest 缺 overrides 声明，Vercel frozen-lockfile 报 ERR_PNPM_OUTDATED_LOCKFILE。受项目「本地禁装」约束未本地跑 install，一致性靠人工核对 lock importers 完成，由 CI 的 lockfile-check 最终验证。

- 修复 CI 失败：mobile lint（tsc --noEmit）报 `TS2304: Cannot find name 'soft'`。位置 `apps/mobile/src/components/toolbox/index.tsx(50,63)`。根因：提交 `7a6e079`「新增心流横屏模式并优化工具箱性能」把内联对象 `soft` 删掉了，但第 50 行仍在引用 `soft[t.color]`（未定义裸标识符）。修复：顶部新增类型安全映射常量 + 补 `import type { Palette } from '@/theme'`：
  ```ts
  const SOFT: Record<ToolEntry['color'], keyof Palette> = {
    primary: 'primarySoft', green: 'greenSoft', orange: 'orangeSoft', blue: 'blueSoft', red: 'redSoft',
  };
  // 使用处：backgroundColor: C[SOFT[t.color]]
  ```
  选枚举映射而非模板字符串拼接 `${color}Soft`，因为 `Palette` 的模板字符串索引不做类型校验；键名与 `Palette` 严格对应，改错会在编译期报错。
  顺手核对 `apps/mobile/src/app/profile.tsx` 的 `C[f.soft]` 无问题——其 `ABOUT_FEATURES` 本地类型显式声明了 `soft: 'primarySoft' | ...` 联合，索引 `Palette` 合法。
  验证：隔离 strict 探针确认表达式类型正确（exit 0）；全量 `tsc --noEmit -p apps/mobile/tsconfig.json` 后 TS2304 报错数为 0，`toolbox/index.tsx` 不再出现在报错列表中。残留的 `TS2307 Cannot find module 'expo-*'/'react-native-*'` 属本机 node_modules 不完整的既有环境噪声（CI 完整，不受影响）。临时目录 `.tmp-typecheck/` 已删除。

- web 端新增知识库阅读区 /library：新增 `apps/web/src/app/library/page.tsx`（左栏 GitHub 笔记目录树：路径过滤/折叠/统计；右栏 Obsidian 全语法渲染阅读：大纲锚点/[[双链]]库内跳转/frontmatter 标签/去工坊编辑入口）+ `apps/web/src/app/api/library/note/route.ts`（服务端 fetchRawFile → renderMarkdown + extractHeadings，含 tags 解析、相对图片路径重写为 raw 直链）。`lib/markdown.ts` md_wikilink 补 data-target 属性（导出产物无副作用）。Sidebar 在知识工坊与语义检索间插入「知识库 📚」。globals.css 追加 .lib-* 排版与 callout/高亮配色（全走主题变量）；坑：`.split > .panel` 的 flex:1 特异性更高，覆盖须挂 `.library-split` 前缀。本机未跑 lint（项目约定 CI 验证）。教训：Edit 工具可能假报成功，批量编辑后需 Grep 复核落盘。

- 知识库区对标 Obsidian 升级：①`/api/library/graph` 全库扫描 [[双链]] 建关系索引（内存缓存 TTL 10min，并发 6；Supabase 不存正文故必须扫 GitHub 原文），三级解析：全路径→文件名→frontmatter 别名；②`LibraryGraph.tsx` 零依赖力导向 SVG 图谱（斥力+弹簧+向心力，requestAnimationFrame 直改 DOM 避免每帧 setState，滚轮缩放/拖拽平移/拖节点/点击打开，顶层目录着色）；③页面新增反链/出链面板、未解析双链置灰标注、Ctrl+K 快速切换器（子序列模糊匹配）、书签+只看书签过滤、最近阅读（localStorage）、字数/阅读时长；④note 接口补 aliases/wordCount/readMinutes。无解析双链未标记到具体来源笔记（unresolved 只记全局次数，因未解析目标无来源边记录）——后续可在扫描时记录 per-note unresolved 改进。

- 知识库优化 + README 更新：**修真 bug**——双链解析曾返回去掉 `.md` 的路径，导致图谱边端点与 nodes.path 对不上（边全被丢弃）、反链匹配恒为空、双链跳转 raw 请求 404；前后端解析统一改为「去 .md 目标 → 完整仓库路径」Map 映射（byPath.get(stripped) 优先），resolveLink 恒返回完整路径。新增 per-note unresolved（graph API `unresolvedBy`），出链面板 ❓ 标注不可跳转的未解析双链；删 LibraryGraph 未用变量 pathToIdx（CI lint 会拦）；CSS 补 .lib-links-col/.lib-link-missing。README 更新：管理台知识库 bullet 写全（三级解析/断链置灰/❓出链/字数时长）、技术取舍补「图谱索引：全库扫描+内存缓存 10min，未落库避动 schema」、TODO 补两条（未解析双链一键创建/图谱落库）。

- web 端搭建 AI 工具区框架 /tools（可插拔架构）：① `apps/web/src/lib/aiTools/registry.ts` —— 双端共享纯数据注册表（AITool/ToolOption 类型 + buildSystemPrompt 选项拼接 + localStorage 历史读写），7 个预置高考工具（笔记润色/考点提炼/智能出题/概念讲解/解题诊断/复习计划/记忆加工），新增工具只加一条定义，页面/API 全自动生效；② `apps/web/src/app/api/tools/run/route.ts` —— 统一执行入口（isAdminRequest 鉴权同工坊精炼闸门、注册表校验 toolId、超长静默截断 + truncated 标记、options 仅取字符串值防注入结构）；③ `apps/web/src/app/tools/page.tsx` —— 'use client' 页面（左栏分类工具卡 sticky、右栏输入/options chips/结果 renderMarkdown 渲染复用 .lib-md 排版/复制/下载 .md/localStorage 历史回看与清空）；④ globals.css 追加 .tools-*（全主题变量驱动 + 900px 断点单列）；⑤ Sidebar MENUS 插入「AI 工具区 🧰」。验证：本地 tsc --noEmit 报错全部来自既有文件（本机 node_modules 缺 mermaid/sql.js/jszip/markdown-it 类型的既有环境噪声，同前一条 /library 的结论），3 个新文件零报错；待 CI 复核。注意本会话 PowerShell stdout 捕获不稳，验证结果须落盘文件再 Read。

- 更新 README.md 同步近期功能：管理台段加知识库阅读区（图谱/Ctrl+K）、AI 工具区（7 工具可插拔）、每日猜题页；云端段加猜题流水线（04:20，降级链/考点锚轮换/预算装配），表数 11→12（含 get_questions_by_key RPC）；目录结构补 library/tools/questions、api tools/run 与 cron/questions、lib agentPolicy/questionSubjects/promptBudget/aiTools registry；AI 悬浮球描述升级为三级审批工作流；部署教程 TAVILY 行补猜题用途 + Cron 时间更新；速查表/故障排查（/api/cron/questions 排查项）/Roadmap 加 Phase 6/TODO 加 4 条待办（审批 UI、审计页、驾驶舱接入猜题、更多科目）/技术取舍加 3 条（审批工作流取舍、token 混合近似口径、材料原文落库不经 LLM 复述）。

- 落地用户提供的「命题 Agent 提示词体系」（9 agent：Filter/Analyst/Adapter/ItemWriter/Reviewer/文言文/论述类/数学变式/真题溯源）到 `apps/web/src/lib/agents/`：① `constants.ts` —— Prompt 与数据分离（八类话题/风险标签/英语干扰项六型/语文九型/文言五型/错因→变式映射/字数规范/评分锚点），改清单改数据不动模板；② `s0.ts` S0 共享前缀（核心认知+铁律，禁止押中承诺/编造/非 JSON 输出）；③ `registry.ts` AgentDef（version 版本化/temperature 逐 agent：Filter·Analyst 0.3、Adapter·ItemWriter 0.75、Reviewer·Tracer 0.2、其余 0.4-0.6/maxOutputTokens：长文 agent 8192 其余 2048-4096/maxInputChars/options 复用 ToolOption 模式），prompt 模板忠实转写用户文本并用 constants 组装；④ `runAgent.ts` 统一封装：S0+options suffix+专属拼接 → promptBudget 预算截断 → JSON 解析失败**回喂原始输出+降温 0.3 重试一次**再 throw 降级 → 返回 {data,version,attempts,inputTruncated}；⑤ llm.ts chatCompletion 加 opts.maxTokens（不传=供应商默认，解决长文输出被 max_tokens 截断产生非法 JSON 的坑）；⑥ /api/agents/run（isAdminRequest 闸门）+ /agents 实验室页（复用 .tools-* 布局、JSON pretty 渲染/复制/下载、localStorage 历史 20 条）+ Sidebar「🎭 命题 Agent」；⑦ schema.sql daily_questions 加 prompt_version text 列（前瞻：流水线升级为多 agent 管线时直接落版本）。两个真实类型错误当场修复（Object.fromEntries 类型收窄、ChatMessage 显式标注）。验证：tsc 总错 32→30（全是既有噪声），agents 文件零报错。待办：cron 猜题流水线升级为 Filter→Adapter→ItemWriter→Reviewer 多 agent 管线（现 registry 为手动实验+后续管线基础）。教训复现：Edit 用「条目开头」做 old_string 锚点会替换掉锚点本身，日志曾把两条目拼在一起，靠 Grep ^- 行首列表发现后修复——追加日志条目应永远用「条目末尾唯一片段」做锚点。

- 将 Codex 算法笔记（J:\WORKSPACE\算法\OpenAI-Codex智能编程代理算法\08-审批工作流、10-提示词预算）落地到高考副驾驶双端：① mobile `lib/agentPolicy.ts` —— 三级审批模型（RiskLevel low/medium/high + ApprovalPolicy auto/suggest/ask + needsApproval 决策函数）、truncateToolOutput token 预算截断（≈2 字符/token 口径）、MMKV 审计日志 pushToolAudit（环形 50 条，decision: auto/confirmed/rejected，argsHint ≤60 字防录敏感全文）；② mobile 接入：settingsStore 加 approvalPolicy（默认 suggest，{...DEFAULTS,...parse} 合并模式自动兼容旧数据）、aiTools.ts 用 TOOL_RISK+toolRisk 取代 WRITE_TOOLS 二元分类（未知工具 fail-safe 按 medium）、aiStore.ts 决策点改 needsApproval（suggest 下 searchWeb/exportNote 也会弹确认卡——行为变化点）、结果回传改 truncateToolOutput(800 tokens)、审计在 confirm/cancel/auto-exec 三处记录（挂起生成卡片时不留痕）；③ web `lib/promptBudget.ts` —— countTokens/clipToBudget（截断带标记）/buildUserPrompt（prefix+input 不可截断，sections 按 priority 降序填充剩余预算、剩余 <100 tokens 整块丢弃、按原顺序拼回）；④ web 接入：pipelines.generateOneSubject 用 buildUserPrompt（QUESTION_PROMPT_BUDGET=6000 tokens，材料 priority 隐含在 input 不可截断、知识库锚点为 section 超预算先截锚点保材料）、/api/tools/run 的字符截断升级为 clipToBudget（maxChars/2 → tokens）。验证：web tsc 32 错全为既有噪声（新文件零命中）；mobile tsc 38 错全为 expo-* 模块噪声（agentPolicy/aiTools/aiStore 改动零类型错误，'@/lib/agentPolicy' 路径解析成功）。待办：approvalPolicy 无 UI（「我的」页后续加三选项）；getToolAuditLog 已导出无消费方（审计查看页后续）。

- web 端搭建每日猜题工作流（知识库考点锚 + 外部时文 → LLM 命题）：① `supabase/schema.sql` 追加 daily_questions 表（unique(user_id,date,subject) 幂等 + RLS 单独 policy + idx_daily_questions_user_date）与移动端免登录 RPC get_questions_by_key(access_key, date)；② `apps/web/src/lib/questionSubjects.ts` 科目命题注册表（searchQuery/sourceLabel/noteKeywords/allowAiMaterial/systemPrompt/questionSpec），预置语文（人民日报等时评→2 单选+1 简答+1 作文猜测）与英语（新闻语料→3 阅读单选+10 空语法填空），LLM JSON 契约 {material?, material_title?, questions[{type,stem,options?,answer?,analysis}], tip} 统一、科目差异收敛在注册表；③ `lib/pipelines.ts` 追加 generateDailyQuestions：fetchArticle（Tavily include_raw_content 抓全文，正文 <500 字弃用，失败/无 Key 降级 AI 自拟材料并要求返回 material 字段）+ pickNoteAnchors（fetchRepoTree 按 noteKeywords 匹配路径→随机 3 篇×2000 字考点锚，每天自然轮换，失败降级空串）→ 逐科目 try/catch（单科目失败不拖垮其他）→ upsert daily_questions（外部素材原文直接落库不经 LLM 复述防失真）；④ `/api/cron/questions`（CRON_SECRET fail-closed 同 daily 模式，部分失败返回 ok:false）+ vercel.json 加 20 20 * * *（北京 04:20，排 daily 04:00 与 knowledge 04:10 之后）；⑤ /questions 阅读页（按日期分组、材料来源链接、题目 renderMarkdown 渲染、答案/立意指导 details 折叠）+ actions.ts regenQuestions 手动触发（Cron 重试只重跑失败科目——已成功科目幂等跳过）。Sidebar 加「每日猜题 🎯」。验证：tsc 报错 32 行中本次改动文件零命中；发现既有 LibraryGraph.tsx 2 个真实类型错误（非本任务引入，会挂 CI，待修）。

- 落地 MyWorkSpace 03-通用增量同步仓库算法到 web 端（PHP/MySQL → TS/Supabase 移植）：① `supabase/schema.sql` tasks 加同步约定列（last_modified timestamptz default now() / is_deleted boolean default false + idx_tasks_user_lm 游标索引）；隔离键映射决策：MyWorkSpace 的 device_id 维度统一为 user_id（单用户多设备收敛同一账号），不增设 device_id 列；② `apps/web/src/lib/syncRepo.ts` —— SyncTableDef 列白名单六型清洗（string/number/boolean/json/stringArray/date，列名+类型都来自白名单=注入免疫）、sanitizeRow（id 缺失 crypto.randomUUID 补、业务列缺字段跳过）、batchUpsert（supabase .upsert onConflict:'id' 单请求多行，user_id/last_modified/is_deleted 服务端强制写入——**服务端权威时钟归一，修复 PHP 版信任客户端时钟的回拨漏拉 trade-off**）、pull（last_modified>since 未删行 ASC + 墓碑 id 列表，limit 1000 分片）、softDelete（is_deleted+last_modified 推进，删除也是变更）；③ `/api/sync/[table]` 通用端点：SYNC_TABLES 表白名单（现注册 tasks{content,subject,status,date}，新表加一条 def 即用）+ x-access-key 鉴权，GET ?since= 增量拉 / POST push / POST delete，响应均带 serverTime 游标；④ 墓碑兼容：现有 tasks 读取方全部补 .eq('is_deleted', false)（/api/tasks/sync 并集与规范池、pipelines 备课/周复盘采集、总览页、insights 画像）。与旧 /api/tasks/sync 并行不破坏移动端；移动端迁移（客户端脏标记/游标/墓碑应用）属 04-多模块同步协调器算法范围待落地。tsc 踩坑：supabase-js 无 schema 泛型时 select 返回 GenericStringError 哨兵类型，需 as unknown as 双段断言。验证：总错 30 全既有噪声，本次文件零报错。
