***

# 📘 高考工作台 · 终极功能蓝皮书

**项目代号**：`Gaokao Co-pilot`（高考副驾驶）
**核心哲学**：**“本地瞬时响应 + 云端永不关机 + 知识资产专业化治理”**

***

## 第一章：系统总体架构（技术栈总览）

| 层级              | 技术选型                                                                  | 核心职责                   |
| :-------------- | :-------------------------------------------------------------------- | :--------------------- |
| **移动端**         | Expo (React Native) + TypeScript                                      | 安卓手机端交互，承载4个Tab及AI悬浮球  |
| **管理台**         | Next.js 14 (App Router) + TypeScript                                  | 专业化知识治理后台，部署于Vercel    |
| **状态管理**        | Zustand (移动端) + Server Actions (管理台)                                  | 轻量且严谨的数据流控制            |
| **本地缓存**        | MMKV                                                                  | 存储会话、离线笔记缓存、当日任务快照     |
| **云端数据库**       | Supabase (PostgreSQL) + pgvector                                      | 结构化数据存储 + 笔记语义向量检索     |
| **文件存储**        | Supabase Storage                                                      | 存储压缩后的错题图片、语音备忘        |
| **AI 引擎**       | DeepSeek API (云端重型生成) + 本地关键词提取                                       | 每日内容生成、画像分析、智能对话       |
| **外部搜索**        | Tavily Search API                                                     | 替代MCP协议，实现外界信息同步与对标    |
| **Markdown 渲染** | Web: `react-markdown` + KaTeX；Mobile: `react-native-markdown-display` | 完美渲染Obsidian笔记、AI回复、公式 |
| **定时任务**        | Vercel Cron Jobs + Supabase Edge Functions                            | 凌晨备课、每周画像复盘、资讯自动检索     |
| **后台唤醒**        | `expo-background-fetch` + `expo-notifications`                        | APP退出后定时轻量同步与本地提醒      |
| **代码执行**        | Pyodide (WebAssembly)                                                 | 手机端本地运行Python代码        |

***

## 第二章：手机端（Expo）—— 4个Tab + 全局AI悬浮球

**全局组件**：底部Tab栏正中间上方悬浮**磨砂玻璃AI球**（直径56pt），点击弹出L4级智能体对话窗口，随点随到，永不遮挡主内容。

### Tab 1：驾驶舱（时间线与当下）

*从上到下精确排布：*

1. **顶部状态栏**：日期 + 天气（OpenWeatherMap，显示“26°C 适合刷题”）。
2. **信仰级倒计时**：超大数字 `距离2026高考 · 280天`，点击切换“X天X小时X分”。
3. **今日提醒横幅**：仅当有自定义日期提醒时出现（如“今日收物理作业”）。
4. **📘 每日知识点（翻转卡片）**：正面标题，点击翻转显示详细推导/笔记。数据来源：AI基于Obsidian凌晨生成。
5. **✏️ 每日一题**：题干展示 + `📝 显示答案` + `🤖 AI讲题`按钮。
6. **今日三件事**：核心任务列表（仅3项），支持与后备箱交换。
7. **🎯 专注模型启动器**：大按钮进入心流模式（全屏黑底倒计时，强制屏蔽通知）。

### Tab 2：弹药库（工具与知识）

*顶部切换标签：`[💻 代码沙盒]`* *|* *`[📓 知识库]`*

- **代码沙盒**：Monaco编辑器 + 运行（Pyodide） + 保存片段 + 强制熔断（5秒无响应杀进程）。
- **知识库**：按需从GitHub拉取目录树，点击单篇下载Markdown，通过Markdown引擎渲染（支持LaTeX、代码高亮、`[[双链]]`跳转）。

### Tab 3：仪表盘（数据与洞察）

*纯Recharts图表区：*

- 学科能力雷达图（五维，100分制）。
- 心流热力图（最佳专注时段）。
- 任务完成率趋势折线图。
- **顶部“我的画像”卡片**：显示优势/危险学科、最近卡壳词云，点击进入全屏画像详情（含横向对标差距）。

### Tab 4：我的（配置与调度）

- API Key配置（DeepSeek）。
- **自定义日期提醒日历**：点击日期添加提醒，红点标记。
- 目标大学设定（用于横向对标）。
- 手动同步按钮（拉取云端最新画像和Obsidian目录）。

***

## 第三章：Web管理台（Next.js）—— 专业化知识操作系统

摒弃传统看板，重塑为三大专业工具：

### 菜单1：知识工坊（治理核心）

- **双侧栏布局**：左侧GitHub文件树（完整Obsidian目录），右侧**Monaco Editor**（VS Code同款，支持Markdown编写、拖拽传图自动压缩WebP）。
- **AI精炼工具栏**：勾选多篇笔记 → 点击“🤖 合并精炼”生成终极复习卡片；点击“🧠 生成知识图谱”输出Mermaid关系图。
- **版本控制**：自动记录每次编辑快照，支持一键回滚至7天前版本。

### 菜单2：语义检索中心（探针式搜索）

- **混合检索框**：支持关键词 + **向量语义检索（pgvector）**。输入“那种需要设辅助函数的导数题”，系统从笔记中语义匹配出最相关3篇（哪怕关键词不匹配）。
- **层级标签管理**：左侧标签树（如 `#数学/微积分/导数`），支持拖拽重命名、合并、批量关联笔记。

### 菜单3：编译与输出（武器库）

- **左侧资源池**：勾选任意笔记、错题、代码片段。
- **右侧编译设置**：一键生成 `📄 复习PDF（A4排版）`、`📱 Anki卡片包(.apkg)` 或 `📋 纯文本大纲`。
- **历史记录**：底部显示最近10次编译产物，支持重新下载。

***

## 第四章：核心智能引擎（AI与数据流）

### 1. L4级AI悬浮球（跨模块调度）

预置 **6大核心工具调用**，识别触发句式并执行：

| 工具名称          | 触发句式示例          | 执行动作              |
| :------------ | :-------------- | :---------------- |
| `addTask`     | “把每日一题加到明天后备箱”  | 调用Zustand接口写入数据库  |
| `setReminder` | “提醒我9月10日交数学作业” | 创建日历提醒            |
| `searchWeb`   | “查一下今年物理分数线”    | 调用Tavily API并注入画像 |
| `exportNote`  | “导出这篇笔记为PDF”    | 触发后台编译任务          |
| `queryStats`  | “我最近数学专注多久”     | 查询计时表并返回汇总        |
| `correctCode` | “修复这个报错”        | 读取当前沙盒代码，AI诊断修正   |

**执行前强制确认**：所有写操作弹出确认卡片，防止误触。

### 2. 每日自动化备课流水线（凌晨04:00）

1. **采集**：拉取昨日新增/修改的Obsidian笔记 + 错题库中待复习条目 + 带 `#重点` 标签的内容。
2. **生成**：DeepSeek基于以上素材，同时产出“每日知识点”（100字内+口诀）和“每日一题”（中高难度+分步解析）。
3. **注入画像**：结合外部搜索（Tavily）抓取的“2026考纲变动”，若发现重大题型调整，自动调整本周薄弱点权重。

### 3. 激进模式画像（多维镜像）

- **数据源**：正确率 + 笔记情绪词（“搞不懂”即时加权）+ 语音备忘转文字。
- **输出**：雷达图五维分数 + “危险学科”标签 + 前5个“卡壳术语”词云。
- **横向对标**：搜索目标院校分数线，计算各科差距，显示“🔥 差距20分”及专项建议。

***

## 第五章：数据库终极Schema（10张核心表）

| 表名                       | 核心字段                                                                                  | 职责             |
| :----------------------- | :------------------------------------------------------------------------------------ | :------------- |
| `profiles`               | `user_id`, `target_university`, `target_score`, `learning_style`(jsonb)               | 用户配置与稳定画像      |
| `tasks`                  | `content`, `subject`, `status`(backlog/top3/done), `date`                             | 任务池（支撑三件事+后备箱） |
| `timer_sessions`         | `subject`, `duration`, `efficiency_score`, `interrupt_type`                           | 专注原始记录         |
| `mood_checkins`          | `emoji_code`, `voice_note_url`, `daily_summary`                                       | 情绪与语音备忘        |
| `daily_learning`         | `date`, `knowledge_body`(text), `question_text`, `answer`, `source_note_ids`(text\[]) | AI生成的每日知识与题目   |
| `mistakes`               | `subject`, `tags`(text\[]), `image_urls`, `voice_note_url`                            | 错题本（含语音反思）     |
| `obsidian_metadata`      | `file_path`, `content_hash`, `version_history`(jsonb)                                 | 笔记缓存与版本快照      |
| `knowledge_embeddings`   | `note_id`, `embedding`(vector(1536))                                                  | 笔记向量，支撑语义检索    |
| `knowledge_compilations` | `compiled_type`(pdf/anki/outline), `download_url`, `status`                           | 编译任务记录         |
| `reminders`              | `date`, `content`, `is_expired`                                                       | 自定义日期提醒        |

***

## 第六章：开发阶段划分（Roadmap）

### Phase 1：基础闭环（第1周，可跑通）

- [x] Monorepo初始化 + Supabase项目配置。
- [x] 手机端底部4个Tab空壳搭建。
- [ ] 驾驶舱：倒计时 + 天气 + 今日三件事（纯手动增删改）。
- [ ] 专注模型：基础计时器（无后台）。
- [ ] 管理台：知识工坊（仅文件树预览 + 只读编辑器）。

### Phase 2：智能核心（第2-3周）

- [ ] 弹药库：Pyodide集成 + Obsidian按需下载与Markdown渲染。
- [ ] 全局AI悬浮球（接入DeepSeek API，实现L1-L3级对话）。
- [ ] 凌晨备课流水线（Vercel Cron触发，生成每日知识/一题存入数据库）。
- [ ] 管理台：语义检索中心（pgvector配置 + 基础向量检索）。

### Phase 3：专业化与后台（第4周+）

- [ ] L4级跨模块调度（6大工具调用+确认卡片）。
- [ ] 后台：编译与输出（PDF/Anki导出）。
- [ ] 激进画像系统（雷达图 + 横向对标搜索）。
- [ ] 后台唤醒（expo-background-fetch，实现APP退出后定时轻量同步）。

***

## 第七章：部署与月度成本预估

- **部署**：
  - Next.js → Vercel（Hobby计划，免费）。
  - Supabase（PostgreSQL + Storage + Auth）→ 免费计划（500MB DB + 1GB Storage，个人够用，图片已压缩为WebP）。
  - Tavily API → 每月1000次免费检索（高三一年绰绰有余）。
  - DeepSeek API → 约 ¥0.5/百万token，月均成本 < ¥5。
- **关键运维提醒**：务必开启Supabase的RLS（行级安全策略），确保 `user_id = auth.uid()`，这是你云端数据安全的唯一命门。

***

## 🚀 结语与行动指令

这份《蓝皮书》已覆盖**16项技术选型、10个业务功能模块、10张数据表、7个自动化管道，以及3个开发阶段**。
