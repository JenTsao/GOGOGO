# Expo 官方对 pnpm monorepo 的推荐配置：node_modules 扁平化（npm 式），
# 使 expo prebuild 生成的 gradle 脚本能通过 require.resolve 定位
# react-native / @react-native/gradle-plugin / expo-modules-autolinking（pnpm 默认 symlink 不提升传递依赖，会解析为 null）
node-linker=hoisted
